export interface Book {
    title: string
    author: string
    genre: string
    length: string
    era: string
    pace: string
    narrative: string
}

export interface Question {
    id: number
    question: string
    options: string[]
}